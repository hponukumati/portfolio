import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "Sri Harsha Ponukumati — Software Engineer",
  description:
    "Portfolio of Sri Harsha Ponukumati, software engineer. Projects in speech-to-text, automation, and CLI tooling.",
  openGraph: {
    title: "Sri Harsha Ponukumati — Software Engineer",
    description:
      "Projects in speech-to-text, automation, and CLI tooling.",
    type: "website",
  },
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html
      lang="en"
      className={`${geistSans.variable} ${geistMono.variable} h-full antialiased`}
    >
      <body className="min-h-full flex flex-col">{children}</body>
    </html>
  );
}
